@using OLProject.Models;

	@Session["projectmetatags"]



 

 @if (ViewData["msgStatus"] == null)
     { 
  @using (Html.BeginForm())
     {
        @Html.ValidationSummary(true)
    <table width="95%" align="center">
   
            <tr>
    <td valign="top" align='left'>            <b>Help</b>   </td>    </tr>
           <tr>
    <td align='left'>  
                 @Html.TextAreaFor(model => model.Help)
             <b> @Html.ValidationMessageFor(model => model.Help)</b>
                 </td>
    </tr>
           <tr>
    <td align='left'>  
                    <input type="image" src="../../siteimages/Submit.png" /><br /></td>
    </tr>
    </table>
  }
}
     else
     { 
     <h1>@Html.Raw(ViewData["msgStatus"])</h1>
  }


